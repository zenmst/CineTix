<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uas.cinetix">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AdminPaymentListActivity"></activity>
        <activity android:name=".UploadPaymentActivity" />
        <activity
            android:name=".PaymentActivity"
            android:label="Pembayaran" />
        <activity
            android:name=".SeatPlanActivity"
            android:label="Pilih Kursi" />
        <activity
            android:name=".MovieTimingsActivity"
            android:label="Pilih Jadwal" />
        <activity
            android:name=".DetailActivity"
            android:label="Movie" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".SignPageActivity" />
        <activity android:name=".OnboardingActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>